# PTZ Sample (Generator)

This sample uses the built-in generator to create a ready-to-run scene.

Steps:
1. Open the project in Unity.
2. Run `Tools > Visca > Create PTZ Sample Scene`.
3. Play the generated scene and send VISCA commands to UDP 52381 (default).
